# required metadata

sonar.projectKey=mx-aclaraciones-cs:webresponsive-frontend
sonar.projectName=WebView
sonar.projectVersion=0.0.10

# Modules inherit properties set at parent level
sonar.sourceEncoding=UTF-8
sonar.sources=src

# Set modules IDs, every tecnology needs to define a module
#sonar.modules=<tecnología>-<NombreMódulo#> (sin espacios y para más de un módulo deben ir separados por coma)

# Propiedades del modulo a configurar. El nombre del tecnología-modulo debe concordar con la carpeta de filesystem
#<tecnología>-<NombreMódulo#>.sonar.sources=<OPCIONAL PARA EL PRIMER MODULO. Ruta relativa del código fuente> 
#<tecnología>-<NombreMódulo#>.sonar.projectName=<Nombre del módulo>
#<tecnología>-<NombreMódulo#>.sonar.language=<tecnolog�
# sonar.language=js

sonar.issue.ignore.multicriteria=i1 
sonar.issue.ignore.multicriteria.i1.ruleKey=Web:InternationalizationCheck
sonar.issue.ignore.multicriteria.i1.resourceKey=**/*.html
